type system
===========
[x] number parsing
builtin parsing + eval
	- literals
	- refs
	- builtin fns
	- builtin types (vec, mat)
	- user types (aliases, structs)

nice to have
============
source map
